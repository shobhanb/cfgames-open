/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { apiToggleLockedRequest } from '../../models/api-toggle-locked-request';
import { apiToggleResponse } from '../../models/api-toggle-response';

export interface ToggleLockedHeatsToggleLockedPost$Params {
      body: apiToggleLockedRequest
}

export function toggleLockedHeatsToggleLockedPost(http: HttpClient, rootUrl: string, params: ToggleLockedHeatsToggleLockedPost$Params, context?: HttpContext): Observable<StrictHttpResponse<apiToggleResponse>> {
  const rb = new RequestBuilder(rootUrl, toggleLockedHeatsToggleLockedPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<apiToggleResponse>;
    })
  );
}

toggleLockedHeatsToggleLockedPost.PATH = '/heats/toggle-locked';
