/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { apiTogglePublishedRequest } from '../../models/api-toggle-published-request';
import { apiToggleResponse } from '../../models/api-toggle-response';

export interface TogglePublishedHeatsTogglePublishedPost$Params {
      body: apiTogglePublishedRequest
}

export function togglePublishedHeatsTogglePublishedPost(http: HttpClient, rootUrl: string, params: TogglePublishedHeatsTogglePublishedPost$Params, context?: HttpContext): Observable<StrictHttpResponse<apiToggleResponse>> {
  const rb = new RequestBuilder(rootUrl, togglePublishedHeatsTogglePublishedPost.PATH, 'post');
  if (params) {
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<apiToggleResponse>;
    })
  );
}

togglePublishedHeatsTogglePublishedPost.PATH = '/heats/toggle-published';
