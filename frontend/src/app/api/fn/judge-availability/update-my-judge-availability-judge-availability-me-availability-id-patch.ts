/* tslint:disable */
/* eslint-disable */
/* Code generated by ng-openapi-gen DO NOT EDIT. */

import { HttpClient, HttpContext, HttpResponse } from '@angular/common/http';
import { Observable } from 'rxjs';
import { filter, map } from 'rxjs/operators';
import { StrictHttpResponse } from '../../strict-http-response';
import { RequestBuilder } from '../../request-builder';

import { apiJudgeAvailabilityModel } from '../../models/api-judge-availability-model';
import { apiJudgeAvailabilityUpdate } from '../../models/api-judge-availability-update';

export interface UpdateMyJudgeAvailabilityJudgeAvailabilityMeAvailabilityIdPatch$Params {
  availability_id: string;
      body: apiJudgeAvailabilityUpdate
}

export function updateMyJudgeAvailabilityJudgeAvailabilityMeAvailabilityIdPatch(http: HttpClient, rootUrl: string, params: UpdateMyJudgeAvailabilityJudgeAvailabilityMeAvailabilityIdPatch$Params, context?: HttpContext): Observable<StrictHttpResponse<apiJudgeAvailabilityModel>> {
  const rb = new RequestBuilder(rootUrl, updateMyJudgeAvailabilityJudgeAvailabilityMeAvailabilityIdPatch.PATH, 'patch');
  if (params) {
    rb.path('availability_id', params.availability_id, {});
    rb.body(params.body, 'application/json');
  }

  return http.request(
    rb.build({ responseType: 'json', accept: 'application/json', context })
  ).pipe(
    filter((r: any): r is HttpResponse<any> => r instanceof HttpResponse),
    map((r: HttpResponse<any>) => {
      return r as StrictHttpResponse<apiJudgeAvailabilityModel>;
    })
  );
}

updateMyJudgeAvailabilityJudgeAvailabilityMeAvailabilityIdPatch.PATH = '/judge-availability/me/{availability_id}';
